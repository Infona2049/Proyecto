# ==========================================
# CONFIGURACIÓN DE ECOFACT
# ==========================================
# ⚠️ IMPORTANTE: Copia este archivo como .env ANTES de ejecutar el proyecto
# Comando: copy .env.example .env (Windows) o cp .env.example .env (Linux/Mac)

# ==========================================
# CONFIGURACIÓN DE BASE DE DATOS
# ==========================================z

# OPCIÓN 1: SQLite (desarrollo individual - NO RECOMENDADO para equipos)
# USE_POSTGRESQL=False

# OPCIÓN 2: PostgreSQL Local (un miembro del equipo hace de servidor - NO RECOMENDADO)
# USE_POSTGRESQL=True
# DB_NAME=ecofact
# DB_USER=postgres
# DB_PASSWORD=123
# DB_HOST=localhost
# DB_PORT=5432

# OPCIÓN 3: Neon (base de datos en la nube compartida - CONFIGURACIÓN ACTUAL)
# ✅ RECOMENDADO PARA EQUIPOS: Todos usan la misma base de datos
# ⚠️ SOLICITA ESTAS CREDENCIALES AL LÍDER DEL EQUIPO - NO LAS PUBLIQUES EN GITHUB
USE_POSTGRESQL=True
DB_NAME=neondb
DB_USER=neondb_owner
DB_PASSWORD=TU_PASSWORD_DE_NEON_AQUI
DB_HOST=ep-divine-base-abk7yom6-pooler.eu-west-2.aws.neon.tech
DB_PORT=5432

# ==========================================
# CONFIGURACIÓN DE DJANGO
# ==========================================

# Clave secreta de Django (mantener privada en producción)
# ⚠️ GENERA UNA NUEVA con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=TU_SECRET_KEY_AQUI

# Modo debug (True para desarrollo, False para producción)
DEBUG=True

# Hosts permitidos (separados por coma, sin espacios)
# Ejemplo: localhost,127.0.0.1,midominio.com
ALLOWED_HOSTS=localhost,127.0.0.1

# ==========================================
# CONFIGURACIÓN DE EMAIL (Gmail)
# ==========================================
# ⚠️ SOLICITA ESTAS CREDENCIALES AL LÍDER DEL EQUIPO

EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_HOST_USER=TU_EMAIL_AQUI@gmail.com
EMAIL_HOST_PASSWORD=TU_APP_PASSWORD_DE_GMAIL_AQUI
EMAIL_PORT=587
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=TU_EMAIL_AQUI@gmail.com

# ==========================================
# NOTAS IMPORTANTES
# ==========================================
# 1. NUNCA subas el archivo .env a GitHub (ya está en .gitignore)
# 2. Solicita las credenciales reales al líder del equipo
# 3. Para Gmail, necesitas un "App Password", no tu contraseña normal
#    Cómo obtenerlo: https://support.google.com/accounts/answer/185833
# 4. Los usuarios ya están creados: admin, vendedor, cliente, superadmin
# 5. No necesitas crear base de datos local, usamos Neon compartida